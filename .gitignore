node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

# Graded book (real settled picks) — runtime state, never committed
data/
graded_book.db
graded_book.db-shm
graded_book.db-wal

.env
.env.*
!.env.example

*.log
.DS_Store

# Cached TTS audio (regenerated on demand)
server/audio_cache/
